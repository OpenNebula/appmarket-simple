// Import emotion to create css classes
import { css } from '@emotion/css'

const styles = (theme) => {
  return {
    switchView: css({
      alignSelf: 'flex-end',
      marginBottom: '10px',
    }),
  }
}

export default styles